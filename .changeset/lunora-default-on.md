---
"dotflowy": minor
---

Outline sync now defaults to Lunora for the browser and MCP. Disable with `?lunora-sync=off` or `localStorage` `dotflowy:flag:lunora-sync=off`; set `LUNORA_OUTLINE=0` in Worker env to keep MCP on the classic DO.
