---
"dotflowy": patch
---

Keep typed outline text when sync shape confirmation is late (Settings round-trip no longer looks empty until hard refresh). More menu: actions first, restore Copy as Markdown, drop Migrate to Lunora. Settings beta copy no longer names the sync engine.
