/** Height/opacity expand duration for the sticky subheader band (ms). The
 *  filter suggestion popover waits this long before opening so it doesn't float
 *  at a mid-animation input rect. Keep in sync with Subheader's motion
 *  `transition.duration`. */
export const SUBHEADER_EXPAND_MS = 200;
