# Client-side Sentry DSN (#227). Public-by-design — it ships in the browser
# bundle — so it is committed here, not a secret. Vite loads .env.production for
# `vite build` (the prod bundle); dev never reads it, and instrument.client.ts
# only inits when import.meta.env.PROD is true. Mirror of the wrangler.jsonc
# SENTRY_DSN var used by the Worker + DO.
VITE_SENTRY_DSN=https://b2613a2ced209c174bc083a22b926b5f@o4506518599434240.ingest.us.sentry.io/4511720305917952
