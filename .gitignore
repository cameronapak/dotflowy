node_modules
dist
.output
.vinxi
.wrangler
.DS_Store
*.log
.env
.env.local
.routeTree.gen.tmp*

# Local Worker secrets (wrangler dev)
.dev.vars

# CodeGraph indexes — per-machine, never committed. Covers the app index
# (repo root) and the dedicated Effect index at repos/effect-smol/.codegraph/,
# so a vendored-subtree index never dirties the tree or fights a subtree pull.
.codegraph/
